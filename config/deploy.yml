service: demo-bun-elysia
image: gambala/demo-bun-elysia
servers:
  - <%= ENV["MAIN_SERVER_IP"] %>
labels:
  traefik.http.routers.demo-bun-elysia-web.rule: Host(`elysia.gambala.pro`)
registry:
  server: ghcr.io
  username: gambala
  password:
    - KAMAL_REGISTRY_PASSWORD
healthcheck:
  interval: 10s
  path: /
  cmd: ls -l > /dev/null
builder:
  multiarch: false
